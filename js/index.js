/* -------------------------------------------------------------------------- */
/*                                    Swup                                    */
/* -------------------------------------------------------------------------- */
const swup = new Swup();

$(function () {
    $("#headerMenu").load("/elements/header_menu.html");
});
/* -------------------------------------------------------------------------- */
/*                                    Calc                                    */
/* -------------------------------------------------------------------------- */
document.addEventListener("DOMContentLoaded", () => {
   calc(); 
});

calc = () => { };
function init() {
    if (document.querySelector('#Num1')) {
        calc();
    }
}

init();
swup.on('contentReplaced', init);
/* -------------------------------------------------------------------------- */